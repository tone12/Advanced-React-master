// This is client side config only - don't put anything in here that shouldn't be public!    ||      http://localhost:4444 is our Yoga API
export const endpoint = `http://localhost:4444`;
export const perPage = 4;
